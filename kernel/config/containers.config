# What a container engine run by the agent needs, dockerd and runc measured
# first. cove checks none of it: what the kernel compiles is permitted, and the
# engine comes from the image and says itself what it misses.

# Namespaces and cgroup v2, the controllers runc delegates.
CONFIG_NAMESPACES=y
CONFIG_UTS_NS=y
CONFIG_IPC_NS=y
CONFIG_PID_NS=y
CONFIG_NET_NS=y
CONFIG_USER_NS=y
CONFIG_CGROUPS=y
CONFIG_MEMCG=y
CONFIG_CGROUP_PIDS=y
CONFIG_CPUSETS=y
CONFIG_CGROUP_SCHED=y
CONFIG_FAIR_GROUP_SCHED=y
CONFIG_CFS_BANDWIDTH=y
CONFIG_BLK_CGROUP=y
CONFIG_CGROUP_DEVICE=y
CONFIG_CGROUP_FREEZER=y
CONFIG_CGROUP_CPUACCT=y
# cgroup v2 filters devices through an eBPF program attached by runc.
CONFIG_BPF_SYSCALL=y
CONFIG_CGROUP_BPF=y

CONFIG_SECCOMP=y
CONFIG_SECCOMP_FILTER=y
CONFIG_KEYS=y
CONFIG_POSIX_MQUEUE=y

# The default bridge network: a veth per container on a bridge, NAT out.
CONFIG_VETH=y
CONFIG_BRIDGE=y
CONFIG_BRIDGE_NETFILTER=y
CONFIG_NETFILTER=y
CONFIG_NETFILTER_ADVANCED=y
CONFIG_NF_CONNTRACK=y
CONFIG_NF_NAT=y
# dockerd clears the conntrack entries of a stopped container over netlink;
# without it a stale entry can send UDP to the container that replaced it.
CONFIG_NF_CT_NETLINK=y

# The rules dockerd writes through iptables-nft, the default of current
# distributions and of the docker:dind image.
CONFIG_NF_TABLES=y
CONFIG_NF_TABLES_INET=y
CONFIG_NF_TABLES_IPV4=y
CONFIG_NF_TABLES_IPV6=y
CONFIG_NFT_CT=y
CONFIG_NFT_NAT=y
CONFIG_NFT_MASQ=y
CONFIG_NFT_COMPAT=y
CONFIG_NETFILTER_XTABLES=y
CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=y
CONFIG_NETFILTER_XT_MATCH_CONNTRACK=y
CONFIG_NETFILTER_XT_MARK=y
CONFIG_NETFILTER_XT_NAT=y
CONFIG_NETFILTER_XT_TARGET_MASQUERADE=y

# The IPv6 half of xt_addrtype compiles under IP6_NF_IPTABLES alone: without
# it dockerd cannot set up its IPv6 chains. The tables of iptables-legacy stay
# out, as the kernel deprecates them: an image on iptables-legacy is refused by
# dockerd at start, with a message that names the table.
CONFIG_IP_NF_IPTABLES=y
CONFIG_IP6_NF_IPTABLES=y
