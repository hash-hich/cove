package cli

// ParseRun exposes parseRun to the tests of the package.
var ParseRun = parseRun

// ParseList exposes parseList to the tests of the package.
var ParseList = parseList

// ParseStop exposes parseStop to the tests of the package.
var ParseStop = parseStop
